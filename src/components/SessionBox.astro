---
import type { Session } from '../lib/auth';

// The signed-in strip at the top of every server-rendered page. Prerendered
// pages can't show session state (they're built once for everyone), which
// is why this lives on the SSR pages and not in the header.

interface Props {
  session: Session | null;
  csrf: string;
}

const { session, csrf } = Astro.props;
---
<div class="session-box">
  {session ? (
    <>
      <p>
        Signed in as <strong>{session.username}</strong>
        {session.isAdmin && <span class="session-admin">admin</span>}
        <a class="session-link" href="/membership">My membership</a>
        <a class="session-link" href="/tickets">My tickets</a>
        {session.isAdmin && <a class="session-link" href="/register/lookup">Member check</a>}
      </p>
      <form method="post" action="/logout">
        <input type="hidden" name="csrf" value={csrf} />
        <button class="btn-link" type="submit">Sign out</button>
      </form>
    </>
  ) : (
    <>
      <p>Sign in to join event signups.</p>
      <a class="btn btn-primary btn-sm" href="/login">Sign in with Discord</a>
    </>
  )}
</div>

<style is:global>
  /* surface, border and the notched corner come from site.css */
  .session-box {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    flex-wrap: wrap;
    padding: 0.7rem 1rem;
    margin-bottom: 2rem;
    font-size: 0.9rem;
    color: var(--muted);
  }
  .session-box p {
    margin: 0;
  }
  .session-box strong {
    color: var(--ink);
  }
  .session-admin {
    font-family: var(--display);
    font-weight: 600;
    font-size: 0.68rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--blue);
    border: 1px solid var(--blue);
    padding: 0.1rem 0.4rem;
    margin-left: 0.5rem;
  }
  .session-link {
    margin-left: 0.8rem;
    font-size: 0.85rem;
  }
  .btn-link {
    background: none;
    border: none;
    padding: 0;
    color: var(--blue);
    font: inherit;
    cursor: pointer;
    text-decoration: underline;
  }
  .btn-link:hover {
    color: var(--blue-dark);
  }
  .btn-sm {
    padding: 0.45rem 0.95rem;
    font-size: 0.85rem;
  }
</style>
